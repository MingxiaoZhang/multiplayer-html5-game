export type PlayerGameData = {
    px: number;
    py: number;
    vx: number;
    vy: number;
    playerId: string;
};

export type PlayerRoomData = {
    playerId: string;
    playerName: string;
    character: string;
    roomId: string;
};
